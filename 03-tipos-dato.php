<?php include 'includes/header.php';

// Boolean
$logueado = true;
var_dump($logueado);

// Enteros
$numero = 200;
var_dump($numero);

// Floats
$float = 200.5;
var_dump($float);

// String
$nombre = "Rodrigo"; // Pueden estar dentro de comillas sensillas o dobles pero no se pueden mezclar
var_dump($nombre);

// Arrays
$array = [];
var_dump($array);



include 'includes/footer.php';